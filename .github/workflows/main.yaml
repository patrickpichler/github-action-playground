name: github-action
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        env:
          TEST: ${{ secrets.TEST_SECRET }}
      - run: sh ./scripts/test.sh
